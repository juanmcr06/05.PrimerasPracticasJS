// OBJETO MATH

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5); // Redondea un numero con decimales
resultado = Math.ceil(2.2);  // Redondea para arriba
resultado = Math.floor(2.5); // Redondea para abajo
resultado = Math.sqrt(144); // Saca la raiz cuadrada de un numero
resultado = Math.abs(-200); // Devuelve un numero absoluto (en este caso lo pasa a positivo)
resultado = Math.min( 3, 5, 7, 9, 12); // Devuelve el numero minimo de un listado
resultado = Math.max( 3, 5, 7, 9, 12); // Devuelve el numero máximo de un listado
resultado = Math.random(); // Devulve un numero aleatorio menor que 1
resultado = Math.floor( Math.random() * 30); // De esta manera siempre va a dar mas de 1 y menos de 30



console.log(resultado);